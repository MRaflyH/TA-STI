"""Acquisition, gridding, the join, and the column contract.

    python3 -m gfd.dataset.era5       download ERA5
    python3 -m gfd.dataset.power      download NASA POWER
    python3 -m gfd.dataset.build      build the processed tables
"""
