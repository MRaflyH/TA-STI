"""Ground flash density prediction, tropical and subtropical domains.

Install once with `pip install -e code/`, then every command is
`python3 -m gfd.<subpackage>` and runs from anywhere in the repo.
"""
